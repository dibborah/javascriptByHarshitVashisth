// map method

const numbers = [3,4,5,1,8];

// function SquareNumber(number) {
//     // return number * number;
//     console.log(number * number);
// }

// const squareArray = numbers.map(SquareNumber);
// console.log(squareArray);

// when not returning anything(undefined returns)
// numbers.map(SquareNumber); // in map function if we return nothing "undefined" is returned by default

// const squareArray = numbers.map((number, index) => {
//     // return `index: ${index} and ${number * number}`;
//     return index;
// })
// console.log(squareArray);

const users = [
    {firstName: 'harshit', age: 21},
    {firstName: 'mohit', age: 23},
    {firstName: 'rahul', age: 26},
];

// const userName = users.map(user => user.firstName);
// console.log(userName);

// returning null
// const userName = users.map(user => {
//     return null;
// });

// console.log(userName);



