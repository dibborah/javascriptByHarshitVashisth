// declare constants

const pi = 3.14;

// pi = 3.15;// Cannot re-assign to a constant variable

console.log(pi * 2) // You can perform any calculation in pi but cannot change the value which is pointed by pi constant