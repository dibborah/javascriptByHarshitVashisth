// use const for creating array

// array is stored in heap memory
// the reference(address) of the array is stored in stack(ex: 0x11)

const fruits = ['mango', 'papaya'];
// fruits = ['chiku', 'grapes'];

fruits.push('banana');
console.log(fruits);

// Note: Use const to declare variables when declaring reference data types



